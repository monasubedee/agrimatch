const baseUrl = "http://192.168.100.17:3000";

const userUrl = baseUrl + '/users';

const signupUrl = baseUrl + '/users/signup';

const signinUrl = baseUrl + '/users/signin';

const chatUrl = baseUrl + '/chats';


export {
    baseUrl,
    userUrl,
    signupUrl,
    signinUrl,
    chatUrl

};